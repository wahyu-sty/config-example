apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aodb.fullname" . }}-test-connection"
  labels:
    {{- include "aodb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aodb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
