apiVersion: v1
kind: Service
metadata:
  name: {{ include "aodb.fullname" . }}
  labels:
    {{- include "aodb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aodb.selectorLabels" . | nindent 4 }}
