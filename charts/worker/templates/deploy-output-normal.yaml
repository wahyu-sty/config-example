{{- if .Values.workerOutputNormal.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "worker.fullname" . }}-output-normal
  labels:
    inalix/app.worker: output-normal
    {{- include "worker.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.workerOutputNormal.replicas }}
  selector:
    matchLabels:
      inalix/app.worker: output-normal
      {{- include "worker.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        inalix/app.worker: output-normal
        {{- include "worker.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "worker.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "d.inalix.com/core:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          envFrom:
            - secretRef:
                name: {{ .Values.inalixSecret }}
            - configMapRef:
                name: {{ .Values.inalixEnv }}
          command:
            - /bin/sh
            - -c
            - |
              django-admin run_default_worker
          readinessProbe:
            timeoutSeconds: 10
            periodSeconds: 20
            exec:
              command:
                - /bin/sh
                - -c
                - |
                  celery -A inalix.base.celery_app inspect ping
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
{{- end }}
